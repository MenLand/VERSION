const a = 1;

const gitToken = process.env.GH_TOKEN;
const npmToken = process.env.NPM_TOKEN;

// To run semantic-release, provide tokens via environment variables:
// GH_TOKEN=... NPM_TOKEN=... npx semantic-release --no-ci
